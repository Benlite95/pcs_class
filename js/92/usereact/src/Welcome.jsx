export default function Welcome(/*props*/{ first, last }) {
  //const { first, last } = props;

  //props.firstName = 'Donald';

  return (
    <>
      <h2>Hello {first} {last}</h2>
      <h1>Welcome to React!</h1>
    </>
  );
}
